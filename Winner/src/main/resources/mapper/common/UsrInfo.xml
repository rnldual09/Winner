<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Winner.mapper.CommonMapper">
	
	<!-- 이미지 BLOB 사용하기 위해 반드시 필요함 -->
	<resultMap id="usrProfileMap" type="HashMap">
		<result property="profileExt" column="PROFILE_EXT" />
		<result property="profile" column="PROFILE" jdbcType="BLOB"
			javaType="_byte[]" />
	</resultMap>
	
	<select id="usrProfile" resultMap="usrProfileMap" parameterType="Map">
		SELECT PROFILE, PROFILE_EXT
		FROM USRMST
		WHERE USR_ID = #{usrId}
	</select>
	
</mapper>